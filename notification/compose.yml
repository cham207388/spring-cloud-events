services:
  notification:
    container_name: notification
    build:
      context: .
      dockerfile: Dockerfile
    extends:
      file: ../compose-config.yml
      service: microservice-base

networks:
  events:
    driver: bridge

